.subtitle
  %b
    In group
.group
  .mugshot
    = image_tag @group['mugshot_url'], :align => 'left'
  .full_name
    %b
      = @group['full_name']
  %br{:clear => 'left'}
.send_message
  = link_to 'Send', :controller => '/yammer/messages', :action => 'new', :group_id => @group['id']

= render 'messages_list'

